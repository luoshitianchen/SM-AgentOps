apiVersion: v1
kind: Service
metadata:
  name: sm-agentops
  namespace: sm
  labels:
    app: sm-agentops
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-agentops
  ports:
  - name: http
    port: 8390
    targetPort: http
    protocol: TCP
